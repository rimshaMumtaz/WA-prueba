
import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-root',
  template: '<router-outlet></router-outlet>',
})
export class AppComponent {
// Este componente no tiene ninguna lógica específica en este momento.
  // Puede contener propiedades y métodos adicionales según sea necesario.
}
